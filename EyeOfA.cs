using UnityEngine;
using System.Collections;

public class EyeOfA : MonoBehaviour {
    public GameObject eyeOfA;
    // Use this for initialization
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        //GameObject eye = GameObject.Instantiate(eyeOfA) as GameObject;
        //eye.transform.localScale = new Vector3(size, size, size);
        //eye.transform.position = transform.position;
    }
    }
